<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:id="@+id/saturation_layout_RL"
    android:layout_height="match_parent"
    tools:visibility="gone">

    <TextView
        android:id="@+id/text_view_saturation"
        style="@style/ucrop_TextViewWidgetText"
        tools:text="100%" />

    <com.example.sahityakumarsuman.newmaker.widget.HorizontalProgressWheelView
        android:id="@+id/saturation_scroll_wheel"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/text_view_saturation" />

</RelativeLayout>